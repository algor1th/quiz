[xyz] = replace xyz with parameter
{xyz, abc} = body coded in json with keys xyz, abc


Doku v 1.3(aktuelle Version):
UserServer:
(1.3)	GET /api/users					// Liste der user [{"id","name","score","level"}] <- Jedes valide token	
	GET /api/users/?sortBy=id
	GET /api/users/?sortBy=name
	GET /api/users/?sortBy=score
(1.3)	GET /api/users/?sortBy=level
(1.3)	GET /api/users/[ID]				// user mit ID {"id","name","score","level"} 	<- Jedes valide token
(1.3)	POST /api/users	{name, score, level}			// fügt user hinzu, ID wird vergeben 	<- Adminuser, falls ADMINUSER==0 -> Jeder
(1.3)	PUT /api/users/id {name, score, level}			// ändert Werte für einen User 		<- User selbst, Adminuser falls ADMINUSER==0 -> Jeder
(1.3)	DELETE /api/users/[ID]				// löscht einen User 			<- User selbst, Adminuser falls ADMINUSER==0 -> Jeder
	
	(POST /api/authentication {userID}		// PLATZHALTER FÜR LOGIN!!! generiert/gibt einen authentifizierungstoken für einen user {"id","name","score","token"}	<- Jeder)
	DELETE /api/authentication/[userID]		// löscht den token für einen User			<- User selbst, Adminuser falls ADMINUSER==0 -> Jeder
	GET /api/authentication/[token]			// Validiert einen Token und gibt den user dazu 	<- Jedes validierte token

GameServer:
(1.3)	GET /api/questions				// gibt alle Fragen an		<- Adminuser
(1.3)	GET /api/questions/?containAnswers=true
(1.3)	GET /api/questions/[ID]				// gibt eine expliziete Frage	<- Jedes valide token
(1.6)	GET /api/questions/[ID]/?forRound=[RoundID]	// gibt eine expliziete Frage mit Angabe der Runde für die sie gebraucht wird	<- Jedes valide token
(1.3)	GET /api/questions/[ID]/?containAnswers=true
(1.3)	POST /api/questions {text, categoryID, 
		requiredLevel, score, answerTime}	// fügt eine neue Frage hinzu	<- Adminuser
(1.3)	PUT /api/questions/[ID] {text, categoryID, 
		requiredLevel, score, answerTime}	// editiert eine Frage 		<- Adminuser
(1.3)	DELETE /api/questions/[ID]			// löscht eine Frage 		<- Adminuser

	GET /api/answers				// gibt alle antworten 		<- Adminuser
	GET /api/answers/?forQuestion=[questionID]	// gibt alle Antworten auf eine expliziete Frage	<- Jedes valide token
	GET /api/answers/[ID]				// gibt eine expliziete Antworten	<- Jedes valide token
	POST /api/answers {text, questionID, isCorrect}		// fügt eine neue Antwort hinzu	<- Adminuser
	PUT /api/answers/id {text, questionID, isCorrect}	// editiert eine Antwort 	<- Adminuser
	DELETE /api/answers/[ID]				// löscht eine Antwort 		<- Adminuser

(1.5)	GET /api/games					//gibt alle Spiele. 	<- Adminuser
	GET /api/games/[ID]				//gibt ein explizietes Spiel. 	<- Adminuser oder wenn User im Spiel
	GET /api/games/[ID]/?containsFullHistory=true
(1.2)	GET /api/games/current				//gibt alle Spiele indem sich der authentifizierte Nutzer befindet. <- jeder valide token
(1.2)	POST /api/games/current 			//Erstellt ein Spiel und matched mit einem User der gleichen proficiency <- jeder valide token
(1.2)	POST /api/games/current/?matchWith=[otherPlayer]//Erstellt ein Spiel und matched mit dem eingegeben User <- jeder valide token
	
	GET /api/rounds/[ID]				//gibt eine expliziete Runde. 	<- Adminuser oder wenn User in Runde	

(1.2)	GET /api/rounds/?forGame=gameID			//gibt die aktuelle Runde inder sich der Nutzer befindet, in dem jeweiligen Spiel. 
							//Ist die letzte Runde beendet und der richtige Nutzer an der Reihe wird eine Auswahl über die 3 Kategorien gesendet. Valider token, der im Spiel ist
	
	PUT /api/rounds/[roundID] {answerID}		//gibt eine Antwort zu der gegebenen Runde <- Token der sich in der Runde befindet
(1.2)	PUT /api/rounds/[gameID]  {categoryID}		//wählt die Kategorie aus und bekommt ein neues Spiel mit Fragen zurück <- Token der sich in der Runde befindet

	(DELETE /api/authentication/[token]		//entwertet ein Token eines Nutzers im cache. Wird auschließlich intern verwendet und nicht extern gecallt! <- Jeder)

(1.2&3)	GET /api/categories				// Liste der kategorien [{"id","name", "requiredLevel"}] <- Jedes valide token	
(1.2&3)	GET /api/categories/[ID]			// kategoire mit ID {"id","name", "requiredLevel"} 	<- Jedes valide token
(1.2&3)	POST /api/categories {name, requiredLevel}	// kategorieuser hinzu, ID wird vergeben<- Adminuser, falls ADMINUSER==0 -> Jeder
(1.2&3)	PUT /api/categories/[ID] {name, requiredLevel}	// ändert Werte für eine kategorie	<- Adminuser falls ADMINUSER==0 -> Jeder
(1.2&3)	DELETE /api/users/[ID]				// löscht eine kategorie		<- Adminuser falls ADMINUSER==0 -> Jeder



Tokens for testing:
ot5lyety960667nnkt4x100waf5jys3sz2x_1
2plqwewnhgjlprbouumrzrcvx15oi9lkx3v_2
gzjfneeyujbsxgg3pwc2rvm5169536qtb0h_3
ixt2ili8caxqyh79az24lqfxfl9se4wf8qd_4
t3c1uetgjiwddiox144t70d0nf9idqumjcw_7